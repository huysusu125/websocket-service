server:
  port: 9292
  http2:
    enabled: true
spring:
  application:
    name: websocket
  redis:
    host: localhost
    port: 6379
    timeout: 60000
    password:

redis-websocket:
  websocket-topic: test
